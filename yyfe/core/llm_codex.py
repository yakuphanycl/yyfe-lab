from __future__ import annotations

def make_patch(task_card: str, files_blob: str) -> str:
    # placeholder (wire OpenAI API later)
    raise NotImplementedError("Wire OpenAI API here (unified diff only).")